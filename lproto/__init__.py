"""lproto — shared protocol definitions.

Typed message envelopes, schema contracts.
No runtime dependencies on other lcore/lbus/lgateway packages.
This is the dependency root — import direction: all → lproto, lproto → nothing.
"""
